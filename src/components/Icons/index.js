import { React } from "react";
import IconFeather from "react-native-vector-icons/Feather";

export function ChevronLeft(props) {
    
    return (
        <IconFeather name='chevron-left' size={props}/>
    )
} 